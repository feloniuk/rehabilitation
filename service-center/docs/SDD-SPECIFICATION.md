# SDD Specification: BookOn Clone with Telegram Integration

## Software Design Document v1.0

**Проект:** SaaS Платформа онлайн-бронювання для сфери послуг
**Мова інтерфейсу:** Українська
**Стек:** Laravel 12, PHP 8.2, MySQL, Telegram Bot API + MadelineProto
**Дата:** Січень 2026

---

## 1. EXECUTIVE SUMMARY

### 1.1 Мета проекту
Створення повнофункціональної SaaS-платформи для онлайн-бронювання послуг (салони краси, барбершопи, клініки, реабілітаційні центри) з унікальною інтеграцією Telegram як основного каналу комунікації та продажів.

### 1.2 Ключові відмінності від Bookon.binotel.ua
| Bookon | Наша платформа |
|--------|----------------|
| Телефонія Binotel | Telegram Bot + Userbot |
| SMS-нагадування | Telegram-повідомлення (безкоштовно) |
| Мобільний додаток | Telegram Mini App |
| Web-віджет | Web-віджет + Telegram-бот для запису |
| Фіксоване ціноутворення | Гнучкі тарифи |

---

## 2. ПОТОЧНИЙ СТАН СИСТЕМИ (AS-IS)

### 2.1 Реалізований функціонал

#### 2.1.1 Multi-Tenancy архітектура
```
Status: DONE
```
- [x] Модель `Tenant` з налаштуваннями
- [x] Pivot-таблиця `tenant_user` (role: owner/admin/master/client)
- [x] Модель `Subscription` (Stripe-ready)
- [x] Middleware `TenantMiddleware`, `TenantRoleMiddleware`
- [x] Trait `BelongsToTenant` для автоматичного scoping
- [x] URL-based routing: `/{tenant}/...`

#### 2.1.2 Управління користувачами
```
Status: DONE
```
- [x] Ролі: super_admin, owner, admin, master, client
- [x] CRUD мастерів з графіком роботи (JSON work_schedule)
- [x] CRUD клієнтів
- [x] Профілі мастерів (рейтинг, досвід, сертифікати, спеціальність)
- [x] Автоматична нормалізація телефонів

#### 2.1.3 Послуги та ціноутворення
```
Status: DONE
```
- [x] Модель `Service` (назва, опис, фото, тривалість)
- [x] Pivot `MasterService` (індивідуальна ціна та тривалість для мастера)
- [x] FAQ для послуг (`ServiceFaq`)

#### 2.1.4 Система бронювання
```
Status: DONE
```
- [x] Онлайн-запис на сайті тенанта
- [x] Генерація доступних слотів (враховує графік мастера)
- [x] Створення записів вручну (адмін)
- [x] Статуси: scheduled, completed, cancelled
- [x] Підтвердження записів (`is_confirmed`)
- [x] Скасування записів (з перевіркою 24h)

#### 2.1.5 Telegram інтеграція
```
Status: PARTIAL
```
- [x] Bot API: повідомлення мастерам про нові записи
- [x] MadelineProto (Userbot): bulk-розсилки клієнтам
- [x] Автоматичний резолв chat_id за номером телефону
- [x] Шаблони повідомлень з placeholder'ами
- [x] Логування відправок (`MasterNotificationLog`, `NotificationLog`)
- [ ] Telegram бот для запису клієнтів
- [ ] Telegram Mini App

#### 2.1.6 Адмін-панель тенанта
```
Status: DONE
```
- [x] Dashboard з календарем записів
- [x] Управління мастерами, клієнтами, послугами
- [x] Управління сторінками та текстовими блоками
- [x] Налаштування центру (назва, телефон, адреса)
- [x] Система сповіщень та шаблонів

#### 2.1.7 Super Admin панель
```
Status: PARTIAL
```
- [x] Dashboard зі статистикою тенантів
- [x] CRUD тенантів
- [x] Активація/деактивація тенантів
- [x] Impersonation (вхід як тенант)
- [ ] Управління підписками
- [ ] Фінансова звітність

#### 2.1.8 Platform (Landing)
```
Status: PARTIAL
```
- [x] Головна сторінка платформи
- [x] Сторінка тарифів
- [x] Сторінка можливостей
- [x] Реєстрація нового тенанта
- [x] Авторизація
- [ ] Онбоардинг для нових тенантів
- [ ] Демо-режим

---

## 3. GAP ANALYSIS: BOOKON vs НАША СИСТЕМА

### 3.1 Функціонал Bookon, якого бракує

| Категорія | Функціонал | Пріоритет | Статус |
|-----------|------------|-----------|--------|
| **Клієнти** | Програма лояльності (бонуси, знижки) | HIGH | TODO |
| | Історія відвідувань клієнта | HIGH | TODO |
| | Відгуки та рейтинги від клієнтів | MEDIUM | TODO |
| | Лист очікування | MEDIUM | TODO |
| **Персонал** | Розрахунок зарплати мастерів | HIGH | TODO |
| | Статистика завантаженості мастерів | MEDIUM | TODO |
| | Мобільний додаток для мастерів | LOW | TODO (Mini App) |
| **Фінанси** | Онлайн-оплата (LiqPay/Monobank) | HIGH | TODO |
| | Каса та фінансові операції | HIGH | TODO |
| | Звіти про доходи | HIGH | TODO |
| | Управління витратами | MEDIUM | TODO |
| **Аналітика** | Дашборд з ключовими метриками | HIGH | PARTIAL |
| | Звіт по клієнтах | MEDIUM | TODO |
| | Звіт по послугах | MEDIUM | TODO |
| | Прогнозування завантаженості | LOW | TODO |
| **Комунікації** | Автоматичні нагадування | HIGH | TODO |
| | Email-розсилки | MEDIUM | TODO |
| | Push-сповіщення | LOW | TODO (Mini App) |
| **Інтеграції** | Імпорт даних з інших CRM | MEDIUM | TODO |
| | API для зовнішніх систем | MEDIUM | TODO |
| | Web-віджет для вбудовування | HIGH | TODO |
| **SaaS** | Тарифні плани | HIGH | PARTIAL |
| | Біллінг (Stripe) | HIGH | TODO |
| | Trial період | MEDIUM | PARTIAL |
| | Ліміти по тарифах | MEDIUM | TODO |

---

## 4. АРХІТЕКТУРА СИСТЕМИ (TO-BE)

### 4.1 Загальна архітектура

```
┌─────────────────────────────────────────────────────────────────┐
│                        PLATFORM LAYER                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │   Landing    │  │   Pricing    │  │   Registration/Auth  │  │
│  └──────────────┘  └──────────────┘  └──────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                        TENANT LAYER                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │ Public Site  │  │ Admin Panel  │  │    Billing Portal    │  │
│  │ (Booking)    │  │ (Management) │  │    (Subscription)    │  │
│  └──────────────┘  └──────────────┘  └──────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                      TELEGRAM LAYER                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │  Booking Bot │  │ Notification │  │     Mini App         │  │
│  │  (per tenant)│  │   Userbot    │  │  (Client Portal)     │  │
│  └──────────────┘  └──────────────┘  └──────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                        API LAYER                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │ Public API   │  │ Webhook API  │  │    Widget API        │  │
│  │ (REST)       │  │ (Telegram)   │  │    (Embed)           │  │
│  └──────────────┘  └──────────────┘  └──────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE LAYER                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    MySQL (shared)                        │  │
│  │              + tenant_id isolation                       │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 Модель даних (розширена)

#### 4.2.1 Нові таблиці

```sql
-- Тарифні плани
CREATE TABLE plans (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(255) NOT NULL,
    slug VARCHAR(255) UNIQUE NOT NULL,
    description TEXT,
    price_monthly DECIMAL(10,2) NOT NULL,
    price_yearly DECIMAL(10,2),
    features JSON,
    limits JSON, -- {masters: 5, appointments_per_month: 500, storage_mb: 500}
    is_active BOOLEAN DEFAULT TRUE,
    sort_order INT DEFAULT 0,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Програма лояльності
CREATE TABLE loyalty_programs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    name VARCHAR(255) NOT NULL,
    type ENUM('cashback', 'points', 'discount') DEFAULT 'points',
    earn_rate DECIMAL(5,2) DEFAULT 1.00, -- 1% від суми
    redeem_rate DECIMAL(5,2) DEFAULT 1.00, -- 1 бал = 1 грн
    min_redeem_amount DECIMAL(10,2),
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Бонуси клієнтів
CREATE TABLE client_bonuses (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    client_id BIGINT NOT NULL,
    program_id BIGINT NOT NULL,
    balance DECIMAL(10,2) DEFAULT 0,
    total_earned DECIMAL(10,2) DEFAULT 0,
    total_spent DECIMAL(10,2) DEFAULT 0,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Транзакції бонусів
CREATE TABLE bonus_transactions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    client_bonus_id BIGINT NOT NULL,
    appointment_id BIGINT,
    type ENUM('earn', 'redeem', 'expire', 'manual') NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    description VARCHAR(255),
    created_at TIMESTAMP
);

-- Відгуки
CREATE TABLE reviews (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    appointment_id BIGINT NOT NULL,
    client_id BIGINT NOT NULL,
    master_id BIGINT NOT NULL,
    service_id BIGINT NOT NULL,
    rating TINYINT NOT NULL, -- 1-5
    comment TEXT,
    is_visible BOOLEAN DEFAULT TRUE,
    reply TEXT,
    replied_at TIMESTAMP,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Лист очікування
CREATE TABLE waitlist (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    client_id BIGINT NOT NULL,
    master_id BIGINT,
    service_id BIGINT NOT NULL,
    preferred_date DATE,
    preferred_time_from TIME,
    preferred_time_to TIME,
    notes TEXT,
    status ENUM('waiting', 'notified', 'booked', 'expired') DEFAULT 'waiting',
    notified_at TIMESTAMP,
    booked_at TIMESTAMP,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Фінансові операції (каса)
CREATE TABLE financial_transactions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    appointment_id BIGINT,
    type ENUM('income', 'expense', 'refund') NOT NULL,
    category_id BIGINT,
    amount DECIMAL(10,2) NOT NULL,
    payment_method ENUM('cash', 'card', 'online', 'bonus') DEFAULT 'cash',
    description VARCHAR(255),
    created_by BIGINT NOT NULL,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Категорії витрат
CREATE TABLE expense_categories (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    name VARCHAR(255) NOT NULL,
    type ENUM('income', 'expense') NOT NULL,
    color VARCHAR(7) DEFAULT '#6B7280',
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Розрахунок зарплат мастерів
CREATE TABLE salary_settings (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    master_id BIGINT NOT NULL,
    calculation_type ENUM('fixed', 'percentage', 'mixed') DEFAULT 'percentage',
    fixed_amount DECIMAL(10,2),
    percentage DECIMAL(5,2) DEFAULT 50.00,
    min_guarantee DECIMAL(10,2),
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

CREATE TABLE salary_calculations (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    master_id BIGINT NOT NULL,
    period_start DATE NOT NULL,
    period_end DATE NOT NULL,
    total_services DECIMAL(10,2) DEFAULT 0,
    total_bonus DECIMAL(10,2) DEFAULT 0,
    total_deductions DECIMAL(10,2) DEFAULT 0,
    calculated_amount DECIMAL(10,2) NOT NULL,
    paid_amount DECIMAL(10,2) DEFAULT 0,
    status ENUM('draft', 'approved', 'paid') DEFAULT 'draft',
    paid_at TIMESTAMP,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Онлайн-платежі
CREATE TABLE payments (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    appointment_id BIGINT NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    provider ENUM('liqpay', 'monobank', 'stripe') NOT NULL,
    provider_payment_id VARCHAR(255),
    status ENUM('pending', 'processing', 'completed', 'failed', 'refunded') DEFAULT 'pending',
    paid_at TIMESTAMP,
    metadata JSON,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Автоматичні нагадування
CREATE TABLE reminder_rules (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    name VARCHAR(255) NOT NULL,
    trigger_type ENUM('before_appointment', 'after_appointment', 'no_visit') NOT NULL,
    trigger_hours INT NOT NULL, -- годин до/після
    channel ENUM('telegram', 'email', 'sms') DEFAULT 'telegram',
    template_id BIGINT NOT NULL,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

CREATE TABLE scheduled_reminders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    rule_id BIGINT NOT NULL,
    appointment_id BIGINT NOT NULL,
    scheduled_at TIMESTAMP NOT NULL,
    sent_at TIMESTAMP,
    status ENUM('scheduled', 'sent', 'failed', 'cancelled') DEFAULT 'scheduled',
    error_message TEXT,
    created_at TIMESTAMP
);

-- Telegram боти тенантів
CREATE TABLE tenant_telegram_bots (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    bot_token VARCHAR(255) NOT NULL,
    bot_username VARCHAR(255),
    webhook_secret VARCHAR(255),
    is_active BOOLEAN DEFAULT TRUE,
    settings JSON, -- {welcome_message, booking_enabled, ...}
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Чати Telegram (для бота запису)
CREATE TABLE telegram_conversations (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    chat_id VARCHAR(255) NOT NULL,
    user_id BIGINT, -- пов'язаний клієнт
    state VARCHAR(255), -- поточний стан діалогу
    context JSON, -- дані контексту
    last_message_at TIMESTAMP,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Web-віджети
CREATE TABLE widgets (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    name VARCHAR(255) NOT NULL,
    type ENUM('popup', 'inline', 'floating') DEFAULT 'popup',
    settings JSON, -- {colors, position, services[], masters[], ...}
    embed_code TEXT,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Аналітика
CREATE TABLE analytics_daily (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    date DATE NOT NULL,
    appointments_total INT DEFAULT 0,
    appointments_completed INT DEFAULT 0,
    appointments_cancelled INT DEFAULT 0,
    new_clients INT DEFAULT 0,
    returning_clients INT DEFAULT 0,
    revenue DECIMAL(10,2) DEFAULT 0,
    average_check DECIMAL(10,2) DEFAULT 0,
    most_popular_service_id BIGINT,
    most_busy_master_id BIGINT,
    created_at TIMESTAMP,
    updated_at TIMESTAMP,
    UNIQUE KEY (tenant_id, date)
);

-- API ключі для зовнішніх інтеграцій
CREATE TABLE api_keys (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    name VARCHAR(255) NOT NULL,
    key VARCHAR(64) NOT NULL,
    secret_hash VARCHAR(255) NOT NULL,
    permissions JSON, -- ['read:appointments', 'write:appointments', ...]
    last_used_at TIMESTAMP,
    expires_at TIMESTAMP,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

---

## 5. TELEGRAM ІНТЕГРАЦІЯ (ДЕТАЛЬНО)

### 5.1 Архітектура Telegram

```
┌─────────────────────────────────────────────────────────────────┐
│                    TELEGRAM ECOSYSTEM                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              PLATFORM BOT (Один на всю платформу)        │   │
│  │  @BookOnPlatformBot                                      │   │
│  │  - Реєстрація нових тенантів                             │   │
│  │  - Підтримка користувачів                                │   │
│  │  - Маршрутизація до тенант-ботів                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│  ┌───────────────────────────┼───────────────────────────────┐ │
│  │                           ▼                               │ │
│  │  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐   │ │
│  │  │ Tenant Bot 1  │ │ Tenant Bot 2  │ │ Tenant Bot N  │   │ │
│  │  │ @SalonABot    │ │ @BarberBBot   │ │ @ClinicCBot   │   │ │
│  │  │               │ │               │ │               │   │ │
│  │  │ - Запис       │ │ - Запис       │ │ - Запис       │   │ │
│  │  │ - Мої записи  │ │ - Мої записи  │ │ - Мої записи  │   │ │
│  │  │ - Скасування  │ │ - Скасування  │ │ - Скасування  │   │ │
│  │  └───────────────┘ └───────────────┘ └───────────────┘   │ │
│  │              TENANT BOTS (Один на кожного тенанта)        │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              NOTIFICATION USERBOT (Один на платформу)    │   │
│  │  MadelineProto Session                                   │   │
│  │  - Розсилки клієнтам                                     │   │
│  │  - Резолв chat_id за телефоном                           │   │
│  │  - Bulk повідомлення                                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              TELEGRAM MINI APP                           │   │
│  │  web.telegram.org/k/#@{BotUsername}                      │   │
│  │  - Повноцінний інтерфейс запису                          │   │
│  │  - Особистий кабінет клієнта                             │   │
│  │  - Історія відвідувань                                   │   │
│  │  - Бонуси та знижки                                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 Telegram Booking Bot - Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    TELEGRAM BOOKING BOT FLOW                    │
└─────────────────────────────────────────────────────────────────┘

User: /start
Bot:  Вітаємо у {center_name}!
      Оберіть дію:
      [📅 Записатись] [📋 Мої записи] [ℹ️ Про нас]

User: [📅 Записатись]
Bot:  Оберіть послугу:
      [💆 Масаж] [💇 Стрижка] [💅 Манікюр]

User: [💆 Масаж]
Bot:  Оберіть майстра:
      [👤 Олена - ⭐4.9] [👤 Марія - ⭐4.7] [🎲 Будь-який]

User: [👤 Олена]
Bot:  Оберіть дату:
      [📅 Пн 15.01] [📅 Вт 16.01] [📅 Ср 17.01] [➡️ Наступний тиждень]

User: [📅 Вт 16.01]
Bot:  Доступний час:
      [🕐 10:00] [🕐 11:30] [🕐 14:00] [🕐 15:30]

User: [🕐 14:00]
Bot:  ✅ Підтвердіть запис:

      📍 {center_name}
      💆 Масаж
      👤 Олена
      📅 16.01.2026
      🕐 14:00
      💰 800 грн

      [✅ Підтвердити] [❌ Скасувати]

User: [✅ Підтвердити]
Bot:  🎉 Запис підтверджено!

      Надішліть свій контакт для завершення:
      [📱 Надіслати контакт]

User: [📱 Надіслати контакт]
Bot:  ✅ Дякуємо, {client_name}!

      Ваш запис:
      📅 16.01.2026 о 14:00
      💆 Масаж
      👤 Олена

      Нагадаємо за 24 години.

      [📋 Мої записи] [🏠 Головне меню]
```

### 5.3 Telegram Mini App - Screens

```
┌─────────────────────────────────────────────────────────────────┐
│                    TELEGRAM MINI APP SCREENS                    │
└─────────────────────────────────────────────────────────────────┘

1. HOME SCREEN
   ┌─────────────────────────────────────┐
   │  👋 Вітаємо, Олександр!             │
   │                                     │
   │  ┌─────────────────────────────┐   │
   │  │ 🎁 Ваші бонуси: 150 грн     │   │
   │  └─────────────────────────────┘   │
   │                                     │
   │  📅 Найближчий запис:               │
   │  ┌─────────────────────────────┐   │
   │  │ Масаж                        │   │
   │  │ 16.01 о 14:00                │   │
   │  │ Майстер: Олена              │   │
   │  │ [Деталі] [Скасувати]        │   │
   │  └─────────────────────────────┘   │
   │                                     │
   │  ────────────────────────────────   │
   │  [🏠 Головна] [📅 Запис] [👤 Профіль]│
   └─────────────────────────────────────┘

2. BOOKING SCREEN
   ┌─────────────────────────────────────┐
   │  📅 Новий запис                      │
   │                                     │
   │  Послуга:                           │
   │  ┌─────────────────────────────┐   │
   │  │ 💆 Масаж          800 грн   │   │
   │  │ 💇 Стрижка        500 грн   │   │
   │  │ 💅 Манікюр        400 грн   │   │
   │  └─────────────────────────────┘   │
   │                                     │
   │  Майстер: [▼ Оберіть майстра]      │
   │                                     │
   │  Дата: [◀️ 15.01.2026 ▶️]           │
   │                                     │
   │  Час:                               │
   │  [10:00] [11:30] [14:00] [15:30]   │
   │                                     │
   │  [📅 Забронювати]                   │
   └─────────────────────────────────────┘

3. PROFILE SCREEN
   ┌─────────────────────────────────────┐
   │  👤 Мій профіль                      │
   │                                     │
   │  📱 +380671234567                   │
   │  ✏️ Олександр Петренко              │
   │                                     │
   │  ────────────────────────────────   │
   │                                     │
   │  📋 Історія відвідувань             │
   │  ┌─────────────────────────────┐   │
   │  │ 10.01 Масаж - Олена  ✅     │   │
   │  │ 03.01 Стрижка - Марія ✅    │   │
   │  │ 25.12 Масаж - Олена  ✅     │   │
   │  └─────────────────────────────┘   │
   │                                     │
   │  🎁 Бонусна програма                │
   │  Накопичено: 150 грн               │
   │  [Використати бонуси]              │
   │                                     │
   │  ────────────────────────────────   │
   │  [🔔 Сповіщення] [❓ Підтримка]     │
   └─────────────────────────────────────┘
```

---

## 6. API СПЕЦИФІКАЦІЯ

### 6.1 Public API (для віджетів та інтеграцій)

```yaml
openapi: 3.0.0
info:
  title: BookOn Tenant API
  version: 1.0.0

paths:
  /api/v1/{tenant}/services:
    get:
      summary: Список послуг
      parameters:
        - name: tenant
          in: path
          required: true
      responses:
        200:
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Service'

  /api/v1/{tenant}/masters:
    get:
      summary: Список майстрів
      parameters:
        - name: service_id
          in: query
          description: Фільтр за послугою
      responses:
        200:
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Master'

  /api/v1/{tenant}/available-slots:
    get:
      summary: Доступні слоти
      parameters:
        - name: master_id
          in: query
          required: true
        - name: service_id
          in: query
          required: true
        - name: date
          in: query
          required: true
          format: date
      responses:
        200:
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
                  format: time
                  example: "14:00"

  /api/v1/{tenant}/appointments:
    post:
      summary: Створити запис
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppointmentCreate'
      responses:
        201:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Appointment'

  /api/v1/{tenant}/appointments/{id}/cancel:
    patch:
      summary: Скасувати запис
      responses:
        200:
          description: Запис скасовано

components:
  schemas:
    Service:
      type: object
      properties:
        id: { type: integer }
        name: { type: string }
        description: { type: string }
        duration: { type: integer }
        price_from: { type: number }
        photo_url: { type: string }

    Master:
      type: object
      properties:
        id: { type: integer }
        name: { type: string }
        photo_url: { type: string }
        rating: { type: number }
        specialty: { type: string }
        services:
          type: array
          items:
            $ref: '#/components/schemas/MasterService'

    MasterService:
      type: object
      properties:
        service_id: { type: integer }
        price: { type: number }
        duration: { type: integer }

    AppointmentCreate:
      type: object
      required: [master_id, service_id, date, time, client_name, client_phone]
      properties:
        master_id: { type: integer }
        service_id: { type: integer }
        date: { type: string, format: date }
        time: { type: string, format: time }
        client_name: { type: string }
        client_phone: { type: string }
        notes: { type: string }

    Appointment:
      type: object
      properties:
        id: { type: integer }
        status: { type: string, enum: [scheduled, completed, cancelled] }
        date: { type: string, format: date }
        time: { type: string, format: time }
        service: { $ref: '#/components/schemas/Service' }
        master: { $ref: '#/components/schemas/Master' }
```

### 6.2 Webhook API (Telegram)

```yaml
paths:
  /webhook/telegram/{tenant}/{bot_secret}:
    post:
      summary: Telegram Bot Webhook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TelegramUpdate'
      responses:
        200:
          description: OK

  /webhook/payments/liqpay:
    post:
      summary: LiqPay Callback
      requestBody:
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                data: { type: string }
                signature: { type: string }
      responses:
        200:
          description: OK

  /webhook/payments/monobank:
    post:
      summary: Monobank Webhook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MonobankWebhook'
      responses:
        200:
          description: OK
```

---

## 7. ТАРИФНІ ПЛАНИ

### 7.1 Структура тарифів

| Параметр | FREE | STARTER | PROFESSIONAL | ENTERPRISE |
|----------|------|---------|--------------|------------|
| Ціна/міс | 0 грн | 499 грн | 999 грн | 2499 грн |
| Мастерів | 1 | 3 | 10 | Без ліміту |
| Записів/міс | 50 | 300 | 1000 | Без ліміту |
| Telegram бот | ❌ | ✅ | ✅ | ✅ |
| Telegram Mini App | ❌ | ❌ | ✅ | ✅ |
| Онлайн-оплата | ❌ | ❌ | ✅ | ✅ |
| Лояльність | ❌ | ❌ | ✅ | ✅ |
| Зарплата | ❌ | ❌ | ✅ | ✅ |
| Фінанси | ❌ | Базове | ✅ | ✅ |
| Аналітика | Базова | Базова | Розширена | Premium |
| API доступ | ❌ | ❌ | ❌ | ✅ |
| Віджети | 1 | 3 | Без ліміту | Без ліміту |
| Підтримка | Email | Email | Chat | Priority |
| White Label | ❌ | ❌ | ❌ | ✅ |

### 7.2 Trial період

- **Тривалість:** 14 днів
- **Доступ:** Всі функції PROFESSIONAL
- **Автоматично:** Перехід на FREE після закінчення
- **Нагадування:** За 3 дні, за 1 день, в день закінчення

---

## 8. ROADMAP РЕАЛІЗАЦІЇ

### Phase 1: Core Enhancement (4-6 тижнів)
```
Sprint 1-2:
├── Програма лояльності
│   ├── Моделі: LoyaltyProgram, ClientBonus, BonusTransaction
│   ├── Нарахування бонусів при завершенні запису
│   ├── Списання бонусів при оплаті
│   └── UI в адмін-панелі
│
├── Відгуки та рейтинги
│   ├── Модель Review
│   ├── Автоматичний запит відгуку (Telegram)
│   ├── Відображення на сторінці мастера
│   └── Модерація відгуків

Sprint 3-4:
├── Фінансовий модуль
│   ├── Моделі: FinancialTransaction, ExpenseCategory
│   ├── Каса: прихід/витрати
│   ├── Базові звіти
│   └── Експорт в Excel
│
├── Зарплата мастерів
│   ├── Моделі: SalarySettings, SalaryCalculation
│   ├── Калькулятор зарплати
│   ├── Нарахування за період
│   └── Історія виплат
```

### Phase 2: Telegram Booking Bot (3-4 тижні)
```
Sprint 5-6:
├── Telegram Bot інфраструктура
│   ├── Модель TenantTelegramBot
│   ├── Webhook обробник
│   ├── FSM для діалогів
│   └── Модель TelegramConversation
│
├── Booking Flow
│   ├── Вибір послуги
│   ├── Вибір мастера
│   ├── Вибір дати/часу
│   ├── Підтвердження
│   └── Зберігання контакту клієнта

Sprint 7:
├── My Appointments
│   ├── Список записів
│   ├── Деталі запису
│   ├── Скасування
│   └── Перенесення
│
├── Автоматичні нагадування
│   ├── Моделі: ReminderRule, ScheduledReminder
│   ├── Cron job для відправки
│   └── Налаштування в адмін-панелі
```

### Phase 3: Payments & Analytics (4-5 тижнів)
```
Sprint 8-9:
├── Онлайн-оплата
│   ├── Модель Payment
│   ├── LiqPay інтеграція
│   ├── Monobank інтеграція
│   ├── Webhook обробники
│   └── UI часткової/повної оплати

Sprint 10-11:
├── Розширена аналітика
│   ├── Модель AnalyticsDaily
│   ├── Cron для агрегації
│   ├── Dashboard з графіками
│   ├── Звіти: клієнти, послуги, мастери
│   └── Експорт звітів

├── Лист очікування
│   ├── Модель Waitlist
│   ├── Автоматичне сповіщення при звільненні слоту
│   └── UI для клієнтів та адмінів
```

### Phase 4: Telegram Mini App (4-5 тижнів)
```
Sprint 12-14:
├── Mini App Frontend
│   ├── Vue.js / React SPA
│   ├── Telegram Web App SDK
│   ├── Авторизація через initData
│   └── Адаптивний дизайн

├── Mini App Features
│   ├── Повноцінний запис
│   ├── Особистий кабінет
│   ├── Історія відвідувань
│   ├── Бонуси та знижки
│   └── Push-сповіщення
```

### Phase 5: SaaS Infrastructure (3-4 тижні)
```
Sprint 15-16:
├── Біллінг
│   ├── Stripe інтеграція
│   ├── Тарифні плани
│   ├── Підписки
│   ├── Invoices
│   └── Ліміти по тарифах

├── API для інтеграцій
│   ├── Модель ApiKey
│   ├── REST API endpoints
│   ├── Rate limiting
│   └── API документація

Sprint 17:
├── Web-віджети
│   ├── Модель Widget
│   ├── Генератор embed-коду
│   ├── Popup / Inline / Floating
│   └── Кастомізація дизайну
```

### Phase 6: Advanced Features (Ongoing)
```
├── Імпорт з інших CRM
├── White Label
├── Multi-location support
├── Mobile apps (React Native)
├── AI рекомендації
└── Інтеграція з календарями (Google, Apple)
```

---

## 9. ТЕХНІЧНІ ВИМОГИ

### 9.1 Серверні вимоги
- PHP 8.2+
- MySQL 8.0+
- Redis (для черг та кешу)
- Node.js 18+ (для фронтенду)
- Storage: мінімум 50GB SSD

### 9.2 Зовнішні сервіси
- **Telegram Bot API** - безкоштовно
- **MadelineProto** - self-hosted
- **Stripe** - для платежів SaaS
- **LiqPay/Monobank** - для платежів клієнтів
- **AWS S3 / DigitalOcean Spaces** - для файлів

### 9.3 Моніторинг
- Laravel Telescope (development)
- Laravel Horizon (queues)
- Sentry (error tracking)
- Prometheus + Grafana (metrics)

---

## 10. SECURITY CONSIDERATIONS

### 10.1 Data Isolation
- Всі запити фільтруються через `BelongsToTenant` trait
- Global scopes для автоматичної ізоляції
- Перевірка tenant_id на рівні middleware

### 10.2 Authentication
- Laravel Sanctum для API
- Rate limiting на auth endpoints
- 2FA для адмінів (опціонально)

### 10.3 Telegram Security
- Webhook secret validation
- Chat ID verification
- Phone number verification

### 10.4 Payment Security
- PCI DSS compliance (через провайдерів)
- Webhook signature validation
- Secure token storage

---

## APPENDIX A: Міграція існуючих даних

При оновленні з поточної версії:

```bash
# 1. Backup
php artisan backup:run

# 2. Міграції
php artisan migrate

# 3. Seeders для нових даних
php artisan db:seed --class=PlansSeeder
php artisan db:seed --class=ExpenseCategoriesSeeder

# 4. Перегенерація кешу
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

---

## APPENDIX B: Конфігурація Telegram Bot

```php
// config/services.php

'telegram_bot' => [
    'token' => env('TELEGRAM_BOT_TOKEN'),
    'webhook_url' => env('APP_URL') . '/webhook/telegram',
],

'telegram' => [
    'api_id' => env('TELEGRAM_API_ID'),
    'api_hash' => env('TELEGRAM_API_HASH'),
],

'liqpay' => [
    'public_key' => env('LIQPAY_PUBLIC_KEY'),
    'private_key' => env('LIQPAY_PRIVATE_KEY'),
    'sandbox' => env('LIQPAY_SANDBOX', true),
],

'monobank' => [
    'token' => env('MONOBANK_TOKEN'),
    'webhook_secret' => env('MONOBANK_WEBHOOK_SECRET'),
],
```

---

**Document Version:** 1.0
**Last Updated:** January 2026
**Author:** Claude Code Assistant
**Status:** Draft - Pending Review
